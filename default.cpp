/*****************************************************
 * Problem:
 * Author: _thz
 * Date:
 ****************************************************/
#include <cstdio>

#define DEBUG

int main() {
#ifdef DEBUG
  freopen("in.txt", "r", stdin);
  freopen("out.txt", "w", stdout);
#endif  // DEBUG

#ifdef DEBUG
  fclose(stdin);
  fclose(stdout);
#endif  // DEBUG

  return 0;
}